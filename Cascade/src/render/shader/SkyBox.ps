struct VSOutput
{
    float4 pos : SV_POSITION;
    float3 dir : TEXCOORD0;
};

TextureCube texSky : register(t0);
SamplerState sampSky : register(s0);

float4 main(VSOutput input) : SV_Target
{
    return texSky.Sample(sampSky, input.dir);
}
